module.exports = {
	name: [
		"#{names.lastName} #{company.suffix}",
		"#{names.lastName} y #{names.lastName}",
		"#{names.lastName} #{names.lastName} #{company.suffix}",
		"#{names.lastName}, #{names.lastName} y #{names.lastName} Asociados"
	],

	suffix: [
		"S.L.",
		"e Hijos",
		"S.A.",
		"Hermanos"
	]
};